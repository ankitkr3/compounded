---
description: Remove a pin from a skill, allowing automatic demotion to resume. Pass the skill name as $ARGUMENTS.
---

Unpin **$ARGUMENTS**:

!`python3 ${CLAUDE_PLUGIN_ROOT}/scripts/pin_skill.py unpin $ARGUMENTS`
