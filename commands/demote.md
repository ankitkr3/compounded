---
description: Manually demote a skill one tier (.autonomous → .trusted → .verified → .proposed). Pass the skill name as $ARGUMENTS.
---

Demote **$ARGUMENTS** by one tier:

!`python3 ${CLAUDE_PLUGIN_ROOT}/scripts/pin_skill.py demote $ARGUMENTS`
